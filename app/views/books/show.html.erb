<h1><%=@book.title%></h1>
<p>Author(s): <%= @book.authors.first.name %></p>
<p>Pages: <%= @book.number_of_pages %></p>
<p>Year: <%= @book.publish_year %></p>
<img src="<%= @book.cover %>">

<section class="book-stats">
  <h2>Book Stats</h2>
  <div id="top-reviews-one-book">
    <% @book.top_reviews.each do |book_review| %>
    <li><%= book_review.title %></li>
    <li><%= book_review.rating %></li>
    <li><%= book_review.username %></li>
    <% end %>
  </div>
</section>

<h2>Reviews</h2>
<%@book.reviews.each do |review| %>
<p><%= review.title %></p>
<p>User: <%= review.username %></p>
<p>Rating: <%= review.rating %></p>
<p><%= review.review_text %></p>
<% end  %>
